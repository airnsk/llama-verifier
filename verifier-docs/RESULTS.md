# RESULTS.md — верификатор спекулятивных цепочек (llama.cpp)

Проект: отдельный бинарь `llama-verifier` поверх чистой llama.cpp master 73a43d1f.
ТЗ: `docs/SPEC.md` (промт хозяина 06.09 12:00, verbatim). Решения: `docs/DECISIONS.md`.

> **ВНИМАНИЕ:** однокарточные числа V0 (58–144 мс/строку, 208 мс K=1, модель Q4_K_XL,
> ngl=56 с RAM-оффлоадом) — **исторические, не использовать**. С 06.09 15:23 все
> замеры — целевая модель `Qwen3.8-27B-GSQ-RCO-IQ3_S.gguf` и конфиг 2×GPU
> (`-ngl 999`, слой-сплит, весь вес в VRAM).

## Этап V0 — кривая стоимости верификации T(K,N)

Конфиг: 2×CMP 50HX 10GB, `-ngl 999` (5581+7267 MiB), ctx 4096, `-np 5 --rs 0`,
b 512, KV f16 (не квантовать), temp-режим greedy, warm prefix 504 токена
(реальный дамп `t0_code_00`), медиана 3 повторов. Целевая модель GSQ-RCO IQ3_S.

| K×N | 1 | 2 | 4 |
|-----|------|------|------|
| 2 | 59.1 | 83.8 | 133.1 |
| 4 | 76.2 | 122.9 | 164.2 |
| 8 | 119.7 | 150.9 | 231.0 |
| 12 | 136.5 | 171.1 | 255.4 |
| 16 | — (цепочка 16 на K12-прогоне не проверялась отдельно) | | |

мс (verify_ms за раунд). Цена строки: 29.5 мс (2 ряда) → **5.3 мс (48 рядов)**.
Раунд K=8 N=1 ≈ 120 мс. CSV: `bench/` (gsq_T.json cо стенда, v2_T_branch_2gpu.csv —
историческая Q4_K_XL).

## Этап V1 — корректность (гейт бит-эквивалентности)

- `/dbg_batch` A vs B: батч 16 токенов == последовательный ground truth **16/16 строк**
  (D-009): маски и «залипания GDN-состояния» в батче нет.
- Инцидент acc=1: дефект генерации эталона rs-конвейером (D-009, inference-knowledge §15),
  верификатор корректно отверг бракованную цепочку.
- Целевая модель GSQ-RCO, исправленный stateless-построитель: цепочка 16 токенов,
  REPLAY FULL **accepted=16, bonus 17-й**, повтор стабилен (16, 16) — много-токенная
  верификация доказанно эквивалентна последовательной.
- **Внешний гейт (бит-эквивалентность с штатным путём) — ЗАКРЫТ 06.09 16:30:**
  llama-server (этот же plain build, -ngl 999, temp0, seed42, one-pass) сгенерировал
  greedy-16 от того же префикса; деток→ретокенизация верификатором дала 16 id, и они
  **совпали с stateless-цепочкой верификатора 16/16** (первое расхождение — отсутствует).
  `/vverify` на серверных greedy-токенах: accepted=16, bonus=11765 (17-й = тот же, что
  у stateless-цепочки). Оговорка batch-invariance по промту: эквивалентность доказана
  для greedy/temp=0; при temp>0 сравнивать распределения — этап V3.
- Организация прогонов (грабли этого дня): llama-cli со статус-спинером непригоден для
  автоматизации (953 МБ мусора в stdout, зависания); server+verifier ОДНОВРЕМЕННО на
  2×10GB не живут (каждому нужен весь слой-сплит, OOM) — только по очереди.

## V2 — батч сессий (кривая насыщения)

(в работе; профиль N-parallel на фиксированном K)

## V3 — temp>0, V4 — адаптивный K, V5 — регрессии

(по ТЗ, после V1/V2)


## Блок 0 плана V2 (06.09, 17:28–17:46) — гейты перед V2
- 0.1 kv_unified on/off: **ON = референс** (REPLAY 16/16; тёплый повтор 0.001 мс). OFF:
  GGML_ASSERT seq_cp (llama-kv-cache.cpp:506) — веточный режим невозможен. Детали D-011.
- 0.2 1000 тёплых раундов (monotone prefix, chain поверх кэша): полных ре-префиллов 0;
  prefill_ms медиана 48.9 / p95 49.2 / max 137.8 мс; verify_ms медиана 119.8 / p95 120.4.
  Ретракция ниже кэша = полный reset 893–907 мс на X=1..32 несмотря на --rs 18.
  Принято 0/1000 на сырых corpus-цепочках (ожидаемо, a≈0.12) — 1 цепочка из c16-цикла принята 8/8.
- 0.3 --cache-reuse: вне libllama-пути (D-012).

## Инциденты кампании V2-блока 0 (по стандарту 0.3 плана)
- llama-cli: hang на спиннере загрузки + 953 МБ stdout-мусора → убит; заменён на
  llama-server one-pass; все фоны — redirect в файл.
- v1gate4: server+verifier одновременно на 2×10GB → OOM/connection refused; вывод:
  внешние гейты только последовательно.
- `-c 8192 -np 2 --rs 18`: CUDA resource allocation failed (cublasCreate) — rs×ctx не
  влезает в VRAM; rs-эксперименты ограничены c4096.
- HTTP 500 на `--np 1 --rs 18` с chains=1: валидатор веточного режима (D-007) запрещает
  np1+rs — ожидаемое поведение, тест границы перевыпущен на np2.


## V2.1 — кривая насыщения батча сессий (06.09 18:40–18:54, GSQ-RCO-IQ3_S, 2×50HX)

Конфиг: `-c 8192 -np 17 --rs 0 -b 512 --window 8`; сессия = ствол-слот + ветка (2 seq);
8 сессий × префикс 504 (ids504), chain K=8; раунды пулом 16 потоков, 100 раундов/сессию,
3 повтора (разброс повторов <1% — таблица по rep1, он же медианный). Синтетика:
сырые corpus-цепочки (accepted≈0, как в 0.2) — мерим СТОИМОСТЬ, не приёмку.

| S | wall med/p95, мс | verify med, мс | queue med, мс | раундов/с сумма | раундов/с на сессию |
|---|---|---|---|---|---|
| 1 | 126.2 / 126.7 | 116.9 | 8.1 | 7.40 | 7.40 |
| 2 | 157.8 / 158.5 | 147.6 | 8.1 | 10.62 | 5.31 |
| 3 | 189.5 / 190.6 | 178.4 | 8.0 | 12.76 | 4.25 |
| 4 | 221.1 / 222.7 | 209.1 | 8.0 | 14.18 | 3.55 |
| 6 | 282.3 / 285.8 | 269.2 | 7.7 | 16.05 | 2.68 |
| 8 | 347.6 / 352.4 | 333.7 | 7.2 | 17.02 | 2.13 |

Выводы:
- **Модель стоимости подтверждена линейной**: T_verify(S) ≈ 86 + 3.9·(8S) мс — фикс. ~86 мс
  на декод + ~3.9 мс на строку; предельная цена сессии +30.5 мс/раунд, ровная от S=2 до S=8
  (разность соседей 30.7/30.8/30.6/30.0/31.0). **Плато в S≤8 нет** — суммарный throughput
  растёт до S=8 (+130%), сублинейно (8× на 8 сессиях дают 2.3×).
- Для конвейера с реальным драфтером (A~3–4 при K=8): токены/с на сессию ≈ rounds/s × (1+A) —
  узкое место не верификация, а latency-бюджет раунда; лимит сессий выбирать из бюджета:
  S≤4 при требовании <250 мс, S≤8 при <400 мс.
- Ре-префиллов в steady-state 0 (контроль 0.2 держится на сессиях: тёплый раунд
  prefill_ms=0; дельта — бранч-путь p0). Всплески только r0 каждой репы = штатный холодный
  pp 504 (890 мс/сессия; батчем 8×504=4032 токена за 12.1 с ≈ 330 t/s pp).
- errs=0, rejected=0; очередь за весь бег не превысила 2 (enq−batches·S… см. vstats: queue=0
  в пробах); честный 429 не сработал ни разу — при лимите queue>64 или занятой сессии
  (V2.4-тест отдельно).
- KV: 8×(504+8)≈4.1k ячеек из 8192 — потолок по памяти ~15 таких сессий (V2.3 уточнит).

Оговорки к V2.1 (разбор хозяина 06.09 19:18):
- Фикс-часть ~86 мс = 68% раунда при S=1 и 25% при S=8; линейный рост T(S) объясняется НЕделимостью фикса. Потенциально самый дешёвый большой выигрыш — профилировка фикса (nsys/фазовые тайминги: запуск графа, sync двух карт, оверхед декода). В бэклоге как кандидат на «следующий этап» по итогам V2.2 (если цена строки на 16k вырастет кратно — приоритет повышается).
- Цепочки сырые (accepted≈0): реальная нагрузка легче (принятые раунды короче), итоговые t/s в V5 будут выше.
- KV-потолок «~15 сессий» — функция n_ctx=8192 ЭТОГО стенда (2×10 GB), не архитектуры; на production-таргете (P40 2×24 ГБ) таблица V2.3 будет иной.
- Лимит S≤4 (<250 мс) — предварительный до V2.2; если строка на 16k подорожает кратно, кривая сместится и лимиты пересчитываются.


## V2.2/V2.3 — длины префиксов, полная склейка, потолок VRAM (06.09 19:20–19:49, GSQ-RCO-IQ3_S, 2×50HX)

Три режима склейки (окно 8 мс): «липкая» само-синхронизация клиентов (V2.1), джиттер 0–40 мс,
барьер (принудительно полный батч). Стоимость строки от длины контекста:

| L | режим | batch rows | verify med, мс | vs модель 86+3.9·rows |
|---|---|---|---|---|
| 504 | синглтон (K=8, S=1) | 8 | 117.0 | 117.2 ✓ |
| 2048 | синглтон | 8 | 117.8 | +0.8 мс |
| 4096 | синглтон | 8 | 118.8 | +1.8 мс |
| 16384 | синглтон (S=1) | 8 | 122.3 | +5.3 мс (+6%) |
| 16384 | синглтон (S=2, раздельно) | 8 | 126.1 | +9.1 (+9%) |
| 16384 | барьер (S=2, полный батч) | 16 | 165.7 | модель @16k 86+16·4.5=158.6, +4% |
| 504 | барьер (S=8) | 64 | 331.1 | 335.6 ✓ (совпало с V2.1 S=8: 333.7) |

Выводы:
1. **Цена строки почти не зависит от длины контекста**: 504→16k даёт +6–9% на раунд K=8
   (117→122–126 мс), а не кратно. Полная склейка 2×16k (165.7) легла на модель с per-row
   ~4.5 мс @16k vs 3.9 @504 (+15%/строка). Лимиты S≤4/S≤8 не смещаются. Механизм малости
   (гибрид GDN/attention) — гипотеза, не измерялось.
2. **Потолок VRAM (V2.3)**: n_ctx=33792, 2×16k-сессии — штатно, 0 ошибок, 0 ре-префиллов.
   Буферы: KV 2112 MiB (64 KiB/ячейка подтверждена: 33792·64KiB=2112 MiB), RS 2544 MiB
   (на число seq, не токенов), веса 10 827 MiB, compute ~950 MiB → ~16.4 из 19.6 GiB.
   Голова ~3.2 GiB ≈ +52k ячеек: 3×16k (~49k ячеек) — проекция «влезет», 4×16k — на грани.
   На production (P40 2×24) таблица другая — стенд-специфично.
3. **Динамика склейки (для V2.4)**: три режима — (а) липкая: переменные раунды само-
   синхронизируются, 84% раундов в полных батчах (V2.1); (б) джиттер ломает лок, но даёт
   случайные частичные батчи (2.2–4.3 сессии/батч); (в) барьер — детерминированно полные.
   Anti-phase lock (все батчи по 1, enq/batches=1.0) воспроизводим при монотонных раундах —
   риск для latency-бюджета под нагрузкой, честный 429 проверить в V2.4 на липком режиме.
4. **АНОМАЛИЯ (открыто)**: смешанные K (3×8+3×4, 36 строк, барьер) = 338.4 мс — как полный
   S=8 (64 строки), хотя модель предсказывает 226.6. Воспроизведена и без барьера
   (+60–90 мс к модели в обоих смешанных прогонах). Причина НЕ установлена (гипотезы —
   от формы батча до content-зависимости — не проверялись); следующий шаг: бисект
   K4×6 / mixK×6 / K8×6 на одном прогреве + /dbg_batch. До бисекта смешанные-K числа
   считать невалидными.
5. Холодный pp 16k: 51.6–57.4 с (~317 t/s), 16k-ячейки: p0-на-ветке копирует 16 383 ячеек
   за раунд — в тёплом раунде незаметно (v_p95−v_med < 1 мс).

## V2.2-fix — бисект mixed-K и паддинг (06.09 19:59–20:34)

| точка (барьер, L=504) | строк | v_med, мс | модель 86+3.9·rows |
|---|---|---|---|
| UNI-K8×6 | 48 | 266.6 | 273.2 ✓ |
| UNI-K4×6 | 24 | 187.0 | 179.6 ✓ |
| MIXK (3×8+3×4) без паддинга | 36 | 339.2 | 1 ubatch: 226.6 ✗; 2 ubatch: 312.4 (+8.6%) |
| MIXK @ -b 1024 -ub 1024 | 36 | 339.9 | инвариантно → резка структурная (D-015) |
| **MIXK с паддингом до K_max** | 48 (36+12 pad) | **266.2** | 273.2 ✓ → **−73 мс (−21.5%)** |
| UNI-S8 контроль (с паддингом) | 64 | 330.9 | без изменений ✓ |

- `-b 4096 -ub 4096`: «failed to allocate compute pp buffers» — на 2×10 GiB недостижимо.
- Смок корректности (смешанный батч 16+4): A acc=16, B acc=4, тёплые prefill 0.0, анкор 16 — OK.
- Паддинг server-side в коде верификатора; протокол и клиент не менялись.

### Рекомендуемые флаги запуска на данном стенде (2×10 GiB) — заметка прогонщику
- Дефолт: `-c 10240 -np 17 --rs 0 -b 512 --window 8`; без -b флагов лог даёт n_batch=2048, n_ubatch=512. -b/-ub держать ≤1024.
- `-b 4096 -ub 4096` — НЕ ЗАПУСКАТЬ: «failed to allocate compute pp buffers» (pp-буфер не влезает в VRAM при весах 10.8 GiB + KV/RS ~4.6 GiB). Холодный префилл длинных контекстов идёт чанками ≤1024 (эффективно), максимум 2048.
- На production (P40 2×24 GiB) лимит отпадает; таблица V2.3/флаги — стенд-специфичны.

## V2.4 — честный 429 под перегрузом 2× (06.09 20:43–20:49, дефолтные флаги, max_sessions=8)

| фаза | метрика | результат |
|---|---|---|
| O1 лимит слотов | 16 /vopen на 8 слотов | 8×200, 8×429; заголовок `Retry-After: 1` во всех; «no free slots (np/2 sessions in use)» ✓ |
| O2 сырой перегруз (8 сессий × 2 потока, 25 с) | 429-учёт | n429=231, vstats.rejected дельта **231 = ровно** (счётчик точен) |
| O2 | серверная верификация | v_med **329.6** ≈ база S=8 (333.7, −1.2%) — **отклонённые запросы не попадают в батчи** ✓ |
| O2 | клиентский wall | 672 мс (p95 6.8 с) при серверных 330 — см. честную оговорку ниже |
| O3 вежливый перегруз (B уважает Retry-After = 1 с) | wall успешных | **352.0** vs база 347.6 (**+1.3% — деградации нет**) ✓ |
| O3 | голодание B | B выиграл 74 раунда, 66 из них сразу после 429 — контракт работает, голода нет ✓ |
| sanity после перегруза | чистые раунды | все 200, тёплые prefill 0.0, acc=8 стабильно — состояние сессий не тронуто ✓ |

Честная оговорка по O2-wall: клиентский замер (новое TCP-соединение на запрос, 16 GIL-потоков Python) не отделяет артефакт харнеса от реальной стоимости приём-слоя. Серверная истина — v_med (плоская) и учёт 429 (точный). Практический вывод не меняется: **грубые клиенты бьют по себе и по приём-слою, вежливые (Retry-After) не видят никакой деградации** — контракт спроектирован верно.

## Mission checkpoint — сквозная оценка S=1 до V3 (расчёт по измеренным компонентам)
Вход: период раунда верификатора S=1 K=8 = **126 мс** (замер V2.1); драфтер 80 t/s (дано хозяином, не моё измерение) → генерация цепочки 8 токенов = 100 мс; асинхронный конвейер → период = max(126, 100) = 126 мс. Токены/раунд = 1 (бонус) + E[j], E[j] = Σ h^k (k=1..8):

| branch hit rate h | E[j] | токенов/раунд | расчётные t/s |
|---|---|---|---|
| 0.70 | 2.20 | 3.20 | **25.3** |
| 0.80 | 3.33 | 4.33 | **34.3** |
| 0.90 | 5.13 | 6.13 | **48.5** |

- Пол (h→0): 1 токен/126 мс ≈ 7.9 t/s. Драфтер становится узким местом ниже 8/0.126 ≈ **63.5 t/s** (при 80 t/s запас есть).
- Ориентир: D-CFR 29.7–33.2 t/s (одна RTX 3060 12GB, 64K ctx, kadenball/qwen38-27b-rtx3060-dcfr). h≥0.8 уже на уровне/выше ориентира на 2×50HX.
- НЕ в расчёте: реальный h (измерится только в связке с драфтером), tg базовой модели на этом стенде (нужен для «× vs наивного декода»), температурные эффекты (V3). Числа — прогноз по формуле из измеренного периода, не замер сквозной связки.

## Mission checkpoint v2 — якорь на реальных данных Etap 0 (06.09 21:03–21:19)

### h(B): оффлайн по готовым хвостам Etap 0 — это РЕАЛЬНАЯ cross-model приёмка
Пары Etap 0 = [pos, hit_argmax, hit_top5, logp] — тексты MoE-драфтера (Qwen3.6-35B-A3B),
teacher-forced через 27B таргет (etap0_score_doc.py). Схема покрытия хозяина:
{j≥7} ∪ топ-(B−1) точек обрыва; K=8; семантика etap0_agg2 (позиционно-взвешенная
выживаемость S(k), воспроизведена валидацией колонок агрегата дословно).

| домен | h(B=1) | h(B=2) | h(B=3) | **h(B=4)** | h(B=5) | h(B=6) |
|---|---|---|---|---|---|---|
| code | 0.710 | 0.771 | 0.823 | **0.868** | 0.906 | 0.941 |
| ALL | 0.576 | 0.690 | 0.776 | 0.842 | 0.893 | 0.935 |
| prose | 0.450 | 0.608 | 0.729 | 0.818 | 0.881 | 0.930 |
| reasoning | 0.586 | 0.691 | 0.774 | 0.837 | 0.887 | 0.933 |
| refactor | 0.565 | 0.692 | 0.781 | 0.844 | 0.895 | 0.937 |

- **Бар хозяина code ≥ 0.85 при B=4: ВЗЯТ (0.868)**. Оптимальный набор всюду {j≥7, j0, j1, j2}.
- Чувствительность к набору: пример хозяина {2,3,4} → code 0.828 (чуть ниже бара); вариант без j0-ветки → 0.844. Оптимизатор точек обрыва обязателен.
- A8 (одинарная цепочка, E[accepted|K=8]) = **6.37** code@0 / **5.47** ALL@0 (temp0.7: 5.96/5.17) — выше геометрической модели h^k первой миссия-таблицы.

### Наивный TG таргета на стенде (замер)
llama-server (build-plain, НЕ llama-cli — известное зависание), -ngl 999 -c 2048, один поток,
n_predict=256, temp 0, промпт corpus t0_code_00 (512 ток.), 3 прогона после прогрева:
**21.1 t/s медиана** (21.15/21.11/21.10). Выше оценки 12–18.

### Пересчитанная миссия-таблица (реальные A8 + измеренный период)
Токены/раунд = A8+1: 7.37 (code) / 6.47 (ALL); период 126 мс (замер V2.1):
- S=1: **54.5 t/s** code / **47.9** ALL → множитель **2.6× / 2.3×** vs наивный 21.1
- S=2: 39.1 (code) · S=4: 26.2 · S=8: 15.7 (по измеренным раундам/с из V2.1)
- Диапазон хозяина «35–50 t/s при 2–4 сессиях» подтверждается на S=2.
- Флаг V4 (по указанию хозяина): «деградация ниже наивного TG» мониторится с первого
  интеграционного прогона; скользящий j < порога → авто-снижение K / фолбэк на наивный декод.

Статус: неизвестное №1 (branch hit rate) закрыто офлайн, бар взят. Спайк драфтера и синхронная
связка — ПАРКОМ (указание хозяина 21:03); верификатор доводится до конца: V3 → V4 → V5.

## ИТОГ ЭТАПА (06.09 21:26, зафиксировано хозяином)
**верификатор: 2×CMP 50HX, K=8, до 4 сессий, 26–55 t/s/сессию против 21 t/s наивного, контракт перегрузки с 429** — с этой цифрой идём в интеграцию.

Пометки хозяина к миссия-сверке v2:
- Оптимизация набора ветвей (точек обрыва) — **явный параметр конфига драфтера**, дефолт — таблица h(B) из этого расчёта ({j≥7, j0, j1, j2} для B=4).
- Оговорка не теряется: h(B) посчитан из распределений, **не доказан по времени** — драфтер должен успеть сгенерировать цепочку + 3–4 ветви за окно 126 мс (Etap 0: 9 ток = 112 мс на цепочку; ветви короче, длина K−j). Это главный пункт спайка драфтера.
- Наивный TG 21.1 выше оценки 12–18: даже S=4 (26.2 t/s) выше наивного одиночного декода — мультипользовательский режим не отнимает у юзера скорость против соло-запуска (аргумент для сервиса).
- Скоуп V3 УЖАТ (решение хозяина): (1) exact + threshold по спецификации, (2) статистический тест на 20k токенов, (3) БЕЗ полной матрицы по доменам (она известна из Etap 0: agreement temp0.7 ≈ жадному, 0.916 vs 0.938 code). Сэкономленное время — в V4 (фолбэк-флаг «ниже наивного TG» + адаптивный K).
